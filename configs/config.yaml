defaults:
  - model: qwen3_0.6b
  - trainer: subtb
  - task: boxing
  - judge: composite
  - runtime: default
  - _self_

# Global settings
seed: 42
device: auto
output_dir: outputs/${now:%Y-%m-%d_%H-%M-%S}

# WandB (optional)
wandb:
  enabled: false
  project: synthstats
  entity: null
