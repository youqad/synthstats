# uses LocalRunner with inference offloaded to RunPod vLLM

type: runpod

train:
  steps: 1000
  batch_size: 4
  temperature: 0.7
  log_interval: 10

replay:
  capacity: 1000
  ratio: 0.5
  prioritized: true
  alpha: 1.0
