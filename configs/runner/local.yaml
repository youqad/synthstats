# Local PyTorch training on single node.

type: local

train:
  steps: 1000
  batch_size: 4
  temperature: 0.7
  eval_every: 100
  eval_episodes: 8

replay:
  capacity: 1000
  ratio: 0.5
  prioritized: true
  alpha: 1.0
  mode: gfn_rescore  # gfn_rescore | simple
